{
  "name": "waf-panel-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "End-to-end smoke tests for waf-panel via Playwright. Drives a real browser against a docker-compose-booted stack.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
